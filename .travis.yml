
language: swift
osx_image: xcode9.2
xcode_project: Demo.xcodeproj
xcode_scheme: Demo
xcode_sdk: iphonesimulator11.2
sudo: required

env:
  - LC_CTYPE=en_US.UTF-8 LANG=en_US.UTF-8

before_install:
  - brew update

script:
  - xcodebuild -project Demo.xcodeproj -target Demo -sdk iphonesimulator ONLY_ACTIVE_ARCH=NO

after_success:
  - bash <(curl -s https://codecov.io/bash) -J 'Demo' -X xcodeplist
